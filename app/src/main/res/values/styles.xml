<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="DetailCardTextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">22sp</item>
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:layout_marginStart">20dp</item>
    </style>

    <style name="PokemonDetailCardTextView" parent="DetailCardTextView">
        <item name="android:layout_marginStart">10dp</item>
    </style>

    <style name="TypeOfAttackTextView" parent="DetailCardTextView">
        <item name="android:layout_marginStart">30dp</item>
    </style>

    <style name="PokemonTypeOfAttackTextView" parent="DetailCardTextView">
        <item name="android:layout_marginStart">10dp</item>
    </style>

</resources>
